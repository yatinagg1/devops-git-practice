git init --> to initialize git folder it creates .git directory for stores hitory/tracking
git add <filename> --> add filename from untrack to staged state 
git add .  --> to add all files from untrack to staged state
git commit -m <commit>  --> 
git log
git rm --cached <filename>   - to unstage
git status
git restore <filename>
git config --global user.name <username>
git config --global user.email <user email id>
git checkout -b <branchname>   - to make new branch
git switch <branchname>   -- change working branch
git branch -D <branchname>  -- to delete a branch
git checkout <branchname>  -- to change branch
